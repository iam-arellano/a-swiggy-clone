apiVersion: apps/v1
kind: Deployment
metadata:
  name: swiggy-app-deployment
  labels:
    app: swiggy-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: swiggy-app
  template:
    metadata:
      labels:
        app: swiggy-app
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: swiggy-app
        image: raemondarellano/swiggy:1.0.0-9
        imagePullPolicy: "Always"
        ports:
        - containerPort: 3000
