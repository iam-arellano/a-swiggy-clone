apiVersion: v1
kind: Service
metadata:
  name: swiggy-app-service
  labels:
    app: swiggy-app
spec:
  type: NodePort
  ports:  
  - port: 80
    targetPort: 3000
  # selector:
  #   app: swiggy-app




